@page "/"
@model Chirp.Razor.Pages.PublicModel
@using Chirp.Razor.Pages
@using Microsoft.AspNetCore.Identity

@inject SignInManager<Author> SignInManager
@inject UserManager<Author> UserManager
@{
    ViewData["Title"] = "Chirp!";
    Layout = "Shared/_PaginationLayout";
    var routeName = ViewContext.HttpContext.GetRouteValue("author");
    var i = 0;
}

<div>
    <h2> Public Timeline </h2>
    <partial name="_SubmitMessage" />
    <partial name="_sortingDropdown" />

    <p>@ViewData["Error"]</p>
    @if (Model.Cheeps.Any())
    {
        <ul id="messagelist" class="cheeps">
            @foreach (var cheep in Model.Cheeps)
            {
                @if (Model.Sorting == "MostLiked" && i < 3 && Model.CurrentPage == 1)
                {
                    <h1><img src="/images/icon1.png" alt="Icon1" />Chirp!</h1>
                }
                <li>
                    <p>
                        <strong>
                            <a href="/@Uri.EscapeDataString(cheep.Author)">@cheep.Author</a>
                        </strong>
                        @cheep.Message
                        <small>&mdash; @cheep.Timestamp</small>
                        @if (SignInManager.IsSignedIn(User))
                        {
                            <button id="likeBtn-@cheep.CheepId" type="button" class="like-btn @(cheep.UserHasLiked ? "liked" : "")"
                                data-id="@cheep.CheepId" data-url="?handler=Like">
                                @(cheep.UserHasLiked ? "Unlike" : "Like")
                            </button>
                        }
                        <span id="likeCount-@cheep.CheepId">@cheep.LikeCounter</span>
                        <span>Likes</span>
                    <form method="post">
                        @Html.AntiForgeryToken() <!--honestly not sure if i do this right-->
                    </form>
                    </p>
                </li>
                i++;
            }
        </ul>
    }
    else
    {
        <em>There are no cheeps so far.</em>
    }
</div>
@section Scripts {
    <script src="~/js/script.js"></script>
}